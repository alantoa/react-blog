import { createStore } from 'redux'
// import { login } from '../../api/login'

function userLogin(from) {
    // const action = {
        // login()
        // login({state, commit},from).then(res => {
        //     console.log(res)
        // })
    // }

}

export default createStore(userLogin)
